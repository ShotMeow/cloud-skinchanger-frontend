import React from "react";
import { NextPage } from "next";
import Head from "next/head";
import { Layout } from "@/shared/ui";

const SupportPage: NextPage = () => {
  return (
    <Layout>
      <Head>
        <title>Cloud Skinchanger - Поддержка</title>
      </Head>
    </Layout>
  );
};

export default SupportPage;
